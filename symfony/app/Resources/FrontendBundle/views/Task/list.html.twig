{% extends "::base.html.twig" %}

{% block body %}
    <ul>
    {% for task in remaining_tasks %}
        <li>
            {{ task.id }} - {{ task.name }} ({{ task.status }})
            <a href="{{ path('task.update', { taskId: task.id }) }}">[Edit]</a>
            |
            <a href="{{ path('task.complete', { taskId: task.id }) }}">[Done]</a>
            |
            <a href="{{ path('task.delete', { taskId: task.id }) }}">[X]</a>
        </li>
    {% endfor %}

    {% for task in completed_tasks %}
        <li>
            {{ task.id }} - {{ task.name }} ({{ task.status }})
            <a href="{{ path('task.update', { taskId: task.id }) }}">[Edit]</a>
            |
            <a href="{{ path('task.redo', { taskId: task.id }) }}">[Redo]</a>
            |
            <a href="{{ path('task.delete', { taskId: task.id }) }}">[X]</a>
        </li>
    {% endfor %}
    </ul>

    <a href="{{ path('task.create') }}">Create new</a>
    |
    <a href="{{ path('task.clean') }}">Clean</a>
{% endblock %}
